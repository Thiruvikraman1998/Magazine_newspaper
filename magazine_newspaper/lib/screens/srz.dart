import 'package:flutter/material.dart';

class SrzScreen extends StatelessWidget {
  const SrzScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("SRZ Screen"));
  }
}
